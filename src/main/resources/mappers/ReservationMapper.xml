<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kookmin.kookmin.mapper.client.ReservationMapper">
  <select id="findByEmail" resultType="ReservationDto">
    select * from reservation r, user u
    where r.user_id = u.user_id and
    email = #{email}
    order by confirmed_date desc
  </select>
  <select id="findByMentoInfo" resultType="UserDto" parameterType="ReservationDto">
    select u.* from reservation r, mento m, user u
    where r.mento_id = m.mento_id and
    m.user_id = u.user_id and
    reservation_id = #{reservationId}
  </select>
</mapper>